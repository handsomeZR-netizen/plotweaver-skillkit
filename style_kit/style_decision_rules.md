# Style Decision Rules

- 统一使用衬线字体，正文和坐标轴优先 Times New Roman，数学字体用 STIX。
- 优先低饱和配色，不直接沿用 Matplotlib 或 seaborn 默认色盘。
- 网格必须轻，不压过数据本身。
- 图例尽量放在数据区外侧，避免覆盖关键信息。
- 输出默认包含 `png`、`pdf`、`svg`，位图导出 `600 DPI`。
- 项目 B 复用时，先继承主题、留白和颜色哲学，再适配具体图型。
- 如果抽取代码置信度低，允许只复用风格而不复用原始实现细节。
